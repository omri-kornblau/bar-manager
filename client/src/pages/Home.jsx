import React from "react";
import {
  Button
} from "react-bootstrap";

const Home = props => {
  return (
    <div>
      <Button color="primary">
        home
      </Button>
    </div>
  );
}

export default Home;
