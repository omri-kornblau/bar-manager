import React from "react";
import {
  Button
} from "react-bootstrap";

const Temp = props => {
  return (
    <div>
      <Button color="primary">
        about us
      </Button>
    </div>
  );
}

export default Temp;
