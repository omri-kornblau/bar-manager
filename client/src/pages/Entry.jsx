import React from "react";
import {
  Button
} from "react-bootstrap";

const Entry = props => {
  return (
    <div>
      <Button color="primary">
        hello
      </Button>
    </div>
  );
}

export default Entry;
